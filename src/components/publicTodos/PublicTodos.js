import React from 'react';
import { View, Text } from 'react-native';

class PublicTodos extends React.Compnent {
  render() {
    return (
      <View>
      </View>
    );
  }
}

export default PublicTodos;
